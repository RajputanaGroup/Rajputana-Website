Put brand video files here.

  fxstudio-hero.mp4   -> then set data-src on the <video> in /fxstudio/index.html
  skwsh-hero.mp4      -> then set data-src on the <video> in /skwsh/index.html

Keep each file under ~15 MB and 1080p. Anything larger should be hosted on
YouTube (unlisted) or a CDN and embedded instead, so the repo stays small.
GitHub warns above 50 MB per file and blocks at 100 MB.
